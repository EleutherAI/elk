import numpy

# Set line colors and styles
line_colors = ['black', 'blue', 'green', 'orange', 'red', 'maroon', 'salmon']
lr_line_style = ':'
zs_line_style = '--'
ccs_line_style = '-.'
num_hidden_layers = 40
layers_arr = numpy.arange(num_hidden_layers)